import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    paddingTop: 135,
    paddingBottom: 184,
  },
  container: {
    flex: 1,
    paddingHorizontal: 20,
  },
});
